{
  "name": "career-roadmap-ai",
  "scripts": {
    "dev": "concurrently \"npm run frontend\" \"npm run backend\"",
    "frontend": "cd . && npm start",
    "backend": "cd deepseek-backend && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.1"
  }
}
